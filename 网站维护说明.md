# Personal Website 维护说明

网站地址：<https://sshenghu.github.io>  
GitHub 仓库：<https://github.com/sshenghu/sshenghu.github.io>

## 平时主要修改哪里

- 个人简介、Recent News：首页文件 `_pages/about.md`
- 姓名、头像、邮箱、单位、所在地、Scholar 等：`_config.yml`
- 顶部导航栏：`_data/navigation.yml`
- 论文列表：`_publications/`
- 简历页面：`_pages/cv.md`
- 头像和图片：`images/`
- PDF 简历或论文附件：`files/`

文章和页面主要使用 Markdown：普通文字直接写；标题前加 `#`；链接写成 `[显示文字](网址)`。

## 修改后怎样预览

1. 双击根目录中的 `preview.command`。
2. 等终端显示 `Server address: http://127.0.0.1:4000/`。
3. 浏览器打开 <http://127.0.0.1:4000/>。
4. 修改并保存文件后，浏览器通常会自动刷新。
5. 预览结束后，在终端按 `Control + C` 停止服务。

如果浏览器显示 `ERR_CONNECTION_REFUSED`，说明预览服务没有启动，重新双击 `preview.command` 即可。

## 确认无误后怎样发布

在网站文件夹中打开终端，依次运行：

```sh
git status
git add .
git commit -m "Update website"
git push origin master
```

第一次发布时，GitHub 可能要求登录。推送成功后，GitHub Pages 通常会在几分钟内自动更新线上网站。

## 注意事项

- 不要手动修改 `_site/`：它是预览时自动生成的。
- 不要修改 `vendor/` 或 `.bundle/`：它们只是本机运行依赖。
- 发布前先本地预览，尤其要检查链接、头像和 PDF。
- OneDrive 会同步项目，但它不等于发布；只有 `git push` 后线上网站才会更新。
- 大改之前可先复制一份备份，或让我帮忙修改和发布。

